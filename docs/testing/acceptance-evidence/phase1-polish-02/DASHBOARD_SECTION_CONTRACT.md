# POLISH-02 — Dashboard section contract

Zones are labeled regions (`data-owner-zone`, `aria-label`) wrapping existing DASH panels.
Panel `h2`/`headingId` contracts from QA-03 remain authoritative for accessible headings.
Zone titles are visual labels (not duplicate `h2`) to avoid nested heading conflicts.
