# RC5-QA-01 — Secret and artifact review

## Credentials

| Item | Handling |
| --- | --- |
| Owner password | Generated by local seed into gitignored handover; never committed |
| Workflow masking | `scripts/rc5/mask-local-secrets.mjs` emits `::add-mask::` for env keys + handover passwords |
| Repo secrets | No new GitHub secrets for local Owner credentials |
| Production | No `TELEPIZZA_PROD_*`, no Production URLs in executable job steps |

## Artifacts

| Allowed on failure | Excluded |
| --- | --- |
| `playwright-report-rc5-qa-01/` | `scripts/.tmp_pw/**` |
| `test-results/rc5-qa-01/` | `.env`, `.env.local`, `.tmp/supabase.local.env` |
| | Committed storage-state files (none used) |

## Known artifact residual risk

Playwright failure `error-context` / a11y snapshots can include the password **input value** and authenticated cookies inside traces. Artifacts are failure-only, 7-day retention, and must be treated as sensitive. Suite does not commit storage state.

## Executable scan (this slice)

| Pattern | In Owner job / smoke? |
| --- | --- |
| `TELEPIZZA_PROD_` | No (assert refuses if present) |
| `onrender.com` / `telepizza-website.vercel.app` | Docs-only elsewhere; not used as test target |
| Production Supabase host | Refused by env write + run-with-local-env + assert |
| Real Production Owner email | Not used; local seed identity only |
| Committed handover JSON | No (`scripts/.tmp_pw/` gitignored) |
