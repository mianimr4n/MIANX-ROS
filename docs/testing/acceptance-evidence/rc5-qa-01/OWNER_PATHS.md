# RC5-QA-01 — Owner paths

## Fixture

| Item | Value |
| --- | --- |
| Identity | Local seed email `admin@telepizza.pk` (CI-ephemeral only; not Production Owner) |
| Role | `super-admin` |
| Password source | `scripts/.tmp_pw/staff-handover.local.json` (gitignored; generated by `pnpm local:seed`) |
| Repo secrets | None added for credentials |

## Paths tested

| Path | Assertions |
| --- | --- |
| `/admin/login` | Fill local Owner credentials via `browserLogin`; leave login route; Admin modules nav; Super Admin label; Sign out control; no auth loop; no pageerror / required 5xx |
| `/admin/dashboard` | Admin shell; header title matches Owner Command Center / Dashboard; `#admin-main` non-empty; no chunk/runtime error copy |

## Deferred

| Path | Reason |
| --- | --- |
| `/admin/reports` | Optional after core two-path suite is stable; no extra seed required but deferred to limit flake/runtime |

## Mutation exclusions

Suite performs authentication only. Static guard (`owner-smoke-readonly.guard.spec.ts`) asserts smoke source lacks known mutation tokens (order create, refund, payroll, menu update, inventory adjustment, document upload, Production env prefixes).
