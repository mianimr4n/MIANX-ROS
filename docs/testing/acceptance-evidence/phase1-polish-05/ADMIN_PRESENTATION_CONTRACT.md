# Admin presentation contract

Source: `apps/website/client/src/lib/admin-presentation-contract.ts`

Documents typography, spacing, action variants, data states, card kinds, and deferred-disclosure rules.
Does not replace CSS tokens; uses existing `--admin-*` and `--brand-red`.
