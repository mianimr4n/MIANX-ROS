# Accessibility / performance

- Semantic heading + priority/status text
- KPI-aware aria-labels on drill-downs
- 44px targets; Clear filters named
- Reuses dashboard extras — no new polling; mode switch does not refetch
