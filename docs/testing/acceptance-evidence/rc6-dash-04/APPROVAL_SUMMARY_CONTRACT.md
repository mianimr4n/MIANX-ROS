# Approval summary contract

Typed `OwnerApprovalSummary` in `lib/approval-inbox/`. Status vocabulary: PENDING / NEEDS_REVIEW / OVERDUE / BLOCKED (selected types use PENDING). Maturity: DRILL_DOWN only. Counts from existing attention sources — not per-item PII cards.
