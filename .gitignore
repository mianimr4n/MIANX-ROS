node_modules/
.env
.env.*
!.env.example
!.env.*.example
dist/
build/
coverage/
.next/
.expo/
*.log

# Local Windows junction → docs/18-reference/archive/documentation-audit (compat only; never commit)
/_documentation-audit/

# Legacy path (tree archived under docs/18-reference/archive/repository-cleanup-20260712/)
_repository-cleanup/

# Previously never-tracked local cleanup noise (kept on disk under archive; do not inflate git)
docs/18-reference/archive/repository-cleanup-20260712/backups/
docs/18-reference/archive/repository-cleanup-20260712/archive/

# Website app exclusions
apps/website/node_modules/
apps/website/dist/
apps/website/.manus-logs/
apps/website/.env
apps/website/.env.*
!apps/website/.env.example
!apps/website/.env.*.example
apps/website/package-lock.json

# Supabase CLI local state (not project source)
supabase/.temp/
supabase/.branches/

# Supabase CLI
supabase/.branches/
supabase/.temp/
supabase/.cache/
supabase/.DS_Store
.vercel

# Local agent recovery / dumps (never commit)
.phase1-dump/
.restore-phase1/
telepizza/
.mytelepizza-patch-*.json
.apply-mytelepizza.mjs
.dump-*.mjs
.extract-docs.mjs
.restore-phase1.mjs
.test-out*.txt
.test-final.txt

# Local staff password handover (never commit)
scripts/.tmp_pw/

# Local infra scratch (supabase status dumps, cloud env backups)
.tmp/

# Accidental npm lockfile at monorepo root (pnpm is packageManager)
/package-lock.json

# Local one-off acceptance / provisioning scripts (never commit)
scripts/_tmp_*.mjs

# Playwright local run artifacts (if generated)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
